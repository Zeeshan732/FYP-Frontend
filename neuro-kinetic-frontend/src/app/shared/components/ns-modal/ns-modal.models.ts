export type NsModalIntent = 'danger' | 'success' | 'info' | 'warning';

/** `icons` = compact icon footer (default). `text` = labeled buttons using cancelLabel / confirmLabel. */
export type NsModalFooterVariant = 'icons' | 'text';
